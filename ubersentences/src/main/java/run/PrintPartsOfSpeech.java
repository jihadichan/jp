package run;

/**
 * Get a list of all parts of speech in the sentences with counts. For coloring in Anki.
 */
public class PrintPartsOfSpeech {

    /*
      final Map<String, AtomicInteger> posMap = new HashMap<>();
        this.sentences.forEach(sentence -> {
            sentence.getTermInfos().forEach(termInfo -> {
                termInfo.getPartsOfSpeech().forEach(pos -> {
                    final AtomicInteger count = posMap.get(pos);
                    if (count == null) {
                        posMap.put(pos, new AtomicInteger(1));
                    } else {
                        count.incrementAndGet();
                    }
                });
            });
     */

}
